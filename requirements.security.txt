# Security dependencies for RCA-Intelligent-System
# Part of RCA-20: Security Hardening and Secrets Management

# JWT Authentication
python-jose[cryptography]>=3.3.0
PyJWT>=2.8.0
bcrypt>=4.1.0
passlib>=1.7.4

# Secrets Management
hvac>=2.0.0; extra == "vault"  # HashiCorp Vault
boto3>=1.34.0; extra == "aws"  # AWS Secrets Manager

# Rate Limiting
redis>=5.0.0
slowapi>=0.1.9

# PII Detection (optional - enhances redaction)
presidio-analyzer>=2.2.0; extra == "pii"
presidio-anonymizer>=2.2.0; extra == "pii"

# Security Headers and TLS
starlette>=0.35.0
python-multipart>=0.0.6

# Audit Logging (optional database persistence)
# Already included: neo4j driver in requirements.agents.txt
